# Home Assistant skill test

Natural user messages only — see [E2E.md](../E2E.md).

| | |
|--|--|
| **Test file** | `scripts/test/e2e/real/skills/test-home-assistant-e2e.js` |

## Inputs

- List my lights
- What lights do I have?
- List all my devices

Prerequisites: `home-assistant` in `skills.enabled`, `HA_URL` / `HA_TOKEN` in `~/.pasture/.env`, HA reachable.
