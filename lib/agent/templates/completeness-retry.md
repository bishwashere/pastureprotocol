The completeness check found unanswered work: {{MISSING_ITEMS}}.

Use the exposed tools to answer every missing item, then return one concise
user-facing answer. Do not mention this completeness check or list attempted
tool calls. Do not call project-workflow `apply_plan` or `apply_setup` unless
the user explicitly approved dashboard creation.
