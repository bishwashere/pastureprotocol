# Intent Planner

Compatibility prompt wrapper: the current prompt is `turn-router-prompt.md`.

This file intentionally preserves the legacy prompt name for tests and callers
that still load `intent-planner`. Update callers to `turn-router` over time.

